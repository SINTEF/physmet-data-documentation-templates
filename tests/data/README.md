The original data is on SharePoint.
All files in this repo are emptied to 0 bytes. 

I have changed the folder structure slightly. For the SIMS, I added an
additional directory "SIMS/SIMS/", so the instrument name is parsed as SIMS.
Same for Synchrotron and EPMA
